import Camera from "../Camera";
import { Font } from "../font";
import { Layer } from "./layer.interface";

export function createDashboardLayer(font: Font): Layer {
    return (context: CanvasRenderingContext2D, camera: Camera) => {
        //
    }
}